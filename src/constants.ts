// This is replaced with a major version number by webpack during the build process
export const VERSION = '__WEBPACK_VERSION_STUB__'

export const DEFAULT_API_URL = 'https://api.bkpk.io'

export const DEFAULT_URL = `https://${VERSION}bkpk.io`

export const SDK_TAG = '@bkpk/sdk'

export const REQUIRED_SCOPES = ['avatars:read']
